spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/image_data
    username: need_username
    password: need_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
